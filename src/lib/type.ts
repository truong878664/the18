type DynamicSpaces = Partial<{
  DEFAULT: string | number;
  md: string | number;
  lg: string | number;
  xl: string | number;
  "2xl": string | number;
  "3xl": string | number;
}>;

export type { DynamicSpaces };
